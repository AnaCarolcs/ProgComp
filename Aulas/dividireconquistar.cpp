#include <bits/stdc++.h>

using namespace std;

int solve(int a, int b){

  return a + (b - a)/2;

}

int main(){

  int a,b;
  cin >> a >> b;

  vector<int> as(n), bs(n);

  cout << solve(a, b) << endl;

  return 0;
}